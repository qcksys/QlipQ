{
  "name": "qlipq",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp fmt && vp lint && vp run -r test && vp run -r build",
    "dev": "vp run qlipq-website#dev",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "vp run -r build && changeset publish",
    "prepare": "vp config"
  },
  "devDependencies": {
    "@changesets/cli": "^2",
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.9.0"
}
